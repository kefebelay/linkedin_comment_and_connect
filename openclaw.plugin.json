{
  "schema": "openclaw.plugin.v1",
  "name": "linkedin_comment_and_connect",
  "version": "0.1.0",
  "entry": "index.mjs",
  "tools": [
    {
      "name": "linkedin_comment_and_connect",
      "description": "Comment on a LinkedIn post using a logged-in noVNC Chromium via CDP + OS-level xdotool click, then optionally visit a profile URL and send a connection request if available (skips company URLs)."
    }
  ],
  "config": {
    "timeoutSec": {
      "type": "number",
      "default": 1200,
      "description": "Max seconds for synchronous runs."
    },
    "defaultWebhookUrl": {
      "type": "string",
      "default": "",
      "description": "Optional default webhook callback URL."
    },
    "scriptPath": {
      "type": "string",
      "default": "./skills/linkedin-comment-connect/scripts/run_linkedin_comment_connect_hybrid.sh",
      "description": "Path to the runner script (relative to plugin root)."
    }
  }
}
